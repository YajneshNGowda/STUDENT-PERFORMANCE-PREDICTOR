version: "3.9"

services:
  api:
    build: .
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    environment:
      - SENDGRID_API_KEY=${SENDGRID_API_KEY:-}
      - FACULTY_EMAIL=${FACULTY_EMAIL:-faculty@college.edu}
      - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID:-}
      - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN:-}
      - TWILIO_PHONE_FROM=${TWILIO_PHONE_FROM:-}
      - FACULTY_PHONE=${FACULTY_PHONE:-}
      - SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL:-}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  dashboard:
    build: .
    command: streamlit run dashboard/app.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    depends_on:
      - api

  # Uncomment to add model retraining scheduler
  # trainer:
  #   build: .
  #   command: python models/train.py
  #   volumes:
  #     - ./models:/app/models
  #     - ./data:/app/data
  #   profiles: ["train"]
